#include "main.h"

/**
 * print_alphabet - print alphabet
 * check the code.
 *
 * Return: Always 0.
 */
void print_alphabet_x10(void)
{
	int ch;
	int i = 0;

	while (i <= 0)
	{
		for (ch = 'a'; ch <= 'z'; ch++)
		{
			putchar(ch);
		}

		putchar('\n');

	i++;
}
